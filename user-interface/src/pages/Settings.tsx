import { Title1 } from '@fluentui/react-components';

/**
 * Renders the App's settings page.
 * @returns A React node representing the settings page.
 */
export default function Page(): React.ReactNode {
    return (
        <Title1>App Settings</Title1>
    );
}
