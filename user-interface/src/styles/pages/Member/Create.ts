import { makeStyles } from '@fluentui/react-components';

/** List of CSS styles for the Member Create page. */
export const useStyleList = makeStyles({
    'bottomOfPage': { 'marginBottom': '1.5em' },
    'itemSpacing': { 'margin': '1em' },
    'sectionSpacing': {
        'marginBottom': '1.5em',
        'marginTop': '1.5em'
    }
});
