'use client';

import { makeStyles, tokens } from '@fluentui/react-components';

/** List of CSS styles for the application top bar. */
export const useStyleList = makeStyles({
    'default': {
        'backgroundColor': tokens.colorNeutralBackground2Pressed,
        'width': '100%'
    }
});
